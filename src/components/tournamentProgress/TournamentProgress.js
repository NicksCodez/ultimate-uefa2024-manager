import React, { useState } from 'react';

const TournamentProgress = ({
  computerGames,
  winnerComputerTeams,
  results,
  playerTeamName,
}) => {
  const [blah, setBlah] = useState();

  return <div id="tournament-progress">Progress was made</div>;
};

export default TournamentProgress;
